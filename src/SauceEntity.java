import processing.core.PImage;

import java.util.*;

public class SauceEntity extends Entity{
    public SauceEntity(String id, Point position, List<PImage> images) {
        super( id, position, images);
    }

}
